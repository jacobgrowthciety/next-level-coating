# Optional — lets the CLI read the project ID from env instead of hardcoding it in the configs.
# Copy to .env and fill in, or just replace REPLACE_WITH_PROJECT_ID in sanity.config.ts / sanity.cli.ts.
SANITY_STUDIO_PROJECT_ID=your_project_id_here
