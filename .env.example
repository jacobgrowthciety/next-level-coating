# Sanity CMS — project ID from https://sanity.io project settings. Dataset is `production`.
# Copy this file to .env.local and fill in the real value. Also set VITE_SANITY_PROJECT_ID
# in Vercel → Project → Settings → Environment Variables (all environments).
VITE_SANITY_PROJECT_ID=your_project_id_here
